import {RetokenizeAstRecognition, RetokenizedNodes} from './types';

/**
 * retokenize tokens with a ~ as headed char.
 */
export const retokenizeWithBitnotHeadedNSL = (recognition: RetokenizeAstRecognition): RetokenizedNodes => {
	throw 'retokenizeWithBitnotHeadedNSL not supported yet'; // TODO Not supported yet
};

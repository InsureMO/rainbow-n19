import {RetokenizeAstRecognition, RetokenizedNodes} from './types';

/**
 * retokenize tokens with a numeric base part text as headed char(s).
 */
export const retokenizeWithNumericBasePartHeadedNSL = (numericText: string, recognition: RetokenizeAstRecognition): RetokenizedNodes => {
	throw 'retokenizeWithNumberBasePartHeadedNSL not supported yet'; // TODO Not supported yet
};

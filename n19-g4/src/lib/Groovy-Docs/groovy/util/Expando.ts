import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.util.Expando', [
	[ /* class description */
		['b', `Represents a dynamically expandable bean.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

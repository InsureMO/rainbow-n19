import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.util.ClosureComparator', [
	[ /* class description */
		['b', `A Comparator which uses a closure to compare 2 values being equal`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

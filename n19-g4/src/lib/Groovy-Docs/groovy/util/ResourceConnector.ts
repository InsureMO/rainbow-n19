import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.util.ResourceConnector', [
	[ /* class description */
		['b', `Base interface for customizing where resources can be found for the`],
		['i', 'GroovyScriptEngine'],
		['b', `.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.util.BuilderSupport', [
	[ /* class description */
		['b', `Base class for creating arbitrary nested trees of objects or events.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

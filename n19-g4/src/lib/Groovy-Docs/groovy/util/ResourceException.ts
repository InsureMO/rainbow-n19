import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.util.ResourceException', [
	[ /* class description */
		['b', `<dt>See Also:</dt>`],
		['b', `<dd>
<ul class="see-list">
<li><a href="../../serialized-form.html#groovy.util.ResourceException">Serialized Form</a></li>
</ul>
</dd>`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

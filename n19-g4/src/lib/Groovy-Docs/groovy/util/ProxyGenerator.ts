import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.util.ProxyGenerator', [
	[ /* class description */
		['b', `Generates 'Proxy' objects which implement interfaces, maps of closures and/or
 extend classes/delegates.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

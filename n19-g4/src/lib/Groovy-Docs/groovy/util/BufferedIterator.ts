import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.util.BufferedIterator', [
	[ /* class description */
		['b', `An iterator that allows examining the next element without consuming it.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

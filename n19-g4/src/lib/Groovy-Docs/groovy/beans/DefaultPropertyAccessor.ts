import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.beans.DefaultPropertyAccessor', [
	/* class description */ UDF,
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.ClosureException', [
	[ /* class description */
		['b', `An exception thrown by a closure invocation`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

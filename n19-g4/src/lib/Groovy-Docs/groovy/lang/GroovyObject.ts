import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.GroovyObject', [
	[ /* class description */
		['b', `The interface implemented by all Groovy objects.`],
		['b', `
 Especially handy for using Groovy objects when in the Java world.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

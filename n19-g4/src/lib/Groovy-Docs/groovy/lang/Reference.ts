import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.Reference', [
	[ /* class description */
		['b', `Represents a reference to a value`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

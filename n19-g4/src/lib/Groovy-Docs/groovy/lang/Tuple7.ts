import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.Tuple7', [
	[/* class description */
		[/* text */ 't', `Represents a list of 7 typed Objects.`]
	],
	/* class sees */ UDF,
	/* fields */ UDF,
	UDF, UDF
]);

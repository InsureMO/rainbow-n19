import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.Tuple8', [
	[ /* class description */
		['b', `Represents a list of 8 typed Objects.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

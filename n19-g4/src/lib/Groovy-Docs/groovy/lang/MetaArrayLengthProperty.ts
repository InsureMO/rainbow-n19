import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.MetaArrayLengthProperty', [
	[ /* class description */
		['b', `Represents the length property of an array`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

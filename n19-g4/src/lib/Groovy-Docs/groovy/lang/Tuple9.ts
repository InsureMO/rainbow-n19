import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.Tuple9', [
	[ /* class description */
		['b', `Represents a list of 9 typed Objects.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

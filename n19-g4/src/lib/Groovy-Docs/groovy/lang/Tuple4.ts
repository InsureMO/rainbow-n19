import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.Tuple4', [
	[ /* class description */
		['b', `Represents a list of 4 typed Objects.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.MetaExpandoProperty', [
	[ /* class description */
		['b', `Represents a property in an Expando object`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

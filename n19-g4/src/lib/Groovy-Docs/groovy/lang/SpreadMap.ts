import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.SpreadMap', [
	[ /* class description */
		['b', `Helper to turn a list with an even number of elements into a Map.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

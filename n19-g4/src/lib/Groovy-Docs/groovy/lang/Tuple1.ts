import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.Tuple1', [
	[ /* class description */
		['b', `Represents a list of 1 typed Object.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

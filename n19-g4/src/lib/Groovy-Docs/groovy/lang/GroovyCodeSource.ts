import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.GroovyCodeSource', [
	[ /* class description */
		['b', `CodeSource wrapper class that allows specific security policies to be associated with a class
 compiled from groovy source.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

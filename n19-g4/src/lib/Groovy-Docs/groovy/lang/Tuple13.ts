import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.Tuple13', [
	[/* class description */
		[/* text */ 't', `Represents a list of 13 typed Objects.`]
	],
	/* class sees */ UDF,
	/* fields */ UDF,
	UDF, UDF
]);

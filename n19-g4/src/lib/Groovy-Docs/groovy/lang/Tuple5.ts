import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.Tuple5', [
	[/* class description */
		[/* text */ 't', `Represents a list of 5 typed Objects.`]
	],
	/* class sees */ UDF,
	/* fields */ UDF,
	UDF, UDF
]);

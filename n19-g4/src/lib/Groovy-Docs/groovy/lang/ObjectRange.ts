import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.ObjectRange', [
	[ /* class description */
		['b', `Represents an inclusive list of objects from a value to a value using
 comparators.`],
		['b', [['t', `Note: This class is similar to`], ['r', 'groovy.lang.IntRange'], ['t', `. If you make any changes to this
 class, you might consider making parallel changes to`], ['r', 'groovy.lang.IntRange'], ['t', `.`]]],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

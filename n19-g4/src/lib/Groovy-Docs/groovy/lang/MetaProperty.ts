import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.MetaProperty', [
	[ /* class description */
		['b', `Represents a property on a bean which may have a getter and/or a setter`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.EmptyRange', [
	[ /* class description */
		['b', `Constructing Ranges like 0..&lt;0`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

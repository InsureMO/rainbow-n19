import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.PropertyAccessInterceptor', [
	[ /* class description */
		['b', `An interface that adds the ability to intercept
 property getters/setters`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

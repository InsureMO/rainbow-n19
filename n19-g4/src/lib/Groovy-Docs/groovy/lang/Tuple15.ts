import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.Tuple15', [
	[/* class description */
		[/* text */ 't', `Represents a list of 15 typed Objects.`]
	],
	/* class sees */ UDF,
	/* fields */ UDF,
	UDF, UDF
]);

import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.lang.GroovyResourceLoader', [
	[ /* class description */
		['b', `Allows frameworks that integrate with Groovy to determine how Groovy files are resolved.`],
	],
	UDF,
	/* class sees */ UDF,
	UDF, UDF, UDF
]);

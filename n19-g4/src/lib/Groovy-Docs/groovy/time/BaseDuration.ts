import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('groovy.time.BaseDuration', [
	[ /* class description */
		['b', `Base class for date and time durations.`],
	],
	UDF,
	[ /* class sees */ 'groovy.time.Duration'],
	UDF, UDF, UDF
]);

import {UDF} from '../../../utils';
import {DocsCollector} from '../../DocsCollector';

DocsCollector.collect('java.util.EventListener', [
	[/* class description */
		[/* text */ 't', `A tagging interface that all event listener interfaces must extend.`]
	],
	/* fields */ UDF,
	/* constructors */ UDF,
	/* methods */ UDF,
]);

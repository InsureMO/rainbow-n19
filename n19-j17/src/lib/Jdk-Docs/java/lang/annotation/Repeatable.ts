import {UDF} from '../../../../utils';
import {DocsCollector} from '../../../DocsCollector';

DocsCollector.collect('java.lang.annotation.Repeatable', [
	[/* class description */
		[/* text */ 't', `The annotation interface `],
		[/* inline code block */ 'i', `java.lang.annotation.Repeatable`],
		[/* text */ 't', ` is
 used to indicate that the annotation interface whose declaration it
 (meta-)annotates is `],
		[/* text */ 't', `repeatable`],
		[/* text */ 't', `. The value of
 `],
		[/* inline code block */ 'i', `@Repeatable`],
		[/* text */ 't', ` indicates the `],
		[/* text */ 't', `containing annotation
 interface`],
		[/* text */ 't', ` for the repeatable annotation interface.`]
	],
	/* fields */,
	/* constructors */,
	/* methods */,
	/* enum values */ UDF
]);

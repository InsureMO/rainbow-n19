import {UDF} from '../../../../utils';
import {DocsCollector} from '../../../DocsCollector';

DocsCollector.collect('java.nio.channels.ByteChannel', [
	[/* class description */
		[/* text */ 't', `A channel that can read and write bytes.  This interface simply unifies
 `],
		[/* reference */ 'r', `java.nio.channels.ReadableByteChannel`],
		[/* text */ 't', ` and `],
		[/* reference */ 'r', `java.nio.channels.WritableByteChannel`],
		[/* text */ 't', `; it does not
 specify any new operations.`]
	],
	/* fields */ UDF,
	/* constructors */ UDF,
	/* methods */ UDF,
]);

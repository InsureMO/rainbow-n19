import {UDF} from '../../../../../utils';
import {DocsCollector} from '../../../../DocsCollector';

DocsCollector.collect('java.nio.file.attribute.FileStoreAttributeView', [
	[/* class description */
		[/* text */ 't', `An attribute view that is a read-only or updatable view of the attributes of
 a `],
		[/* reference */ 'r', `java.nio.file.FileStore`, `FileStore`],
		[/* text */ 't', `.`]
	],
	/* fields */,
	/* constructors */,
	/* methods */,
	/* enum values */ UDF
]);
